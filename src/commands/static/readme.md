# FHIR Schema Generator:

A custom FHIR Schema code generator.

## Installation

```bash
npm install
npm run build
```

## Usage

```bash
fscg generate -o ./output -f ./path-to-schema-files.ndjson --custom-generator ./path-to-generator
```